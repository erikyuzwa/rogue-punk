/**
 * Options which are passed into creation of a Stage
 * Pretty much a clone of ROT.DisplayOptions
 *
 * https://github.com/ondras/rot.js/blob/master/src/display/types.ts#L3
 */
export interface StageOptions {
    id?: string;
    width?: number;
    height?: number;
}
